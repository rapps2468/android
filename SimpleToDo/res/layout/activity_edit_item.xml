<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:label="@string/title_activity_edit_item"
    tools:context=".EditItemActivity" >

    <TextView
        android:id="@+id/tvEditItemsBelow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Edit Item Below:"
        android:textSize="23sp" />

    <EditText
        android:id="@+id/etEditItem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/tvEditItemsBelow"
        android:layout_below="@+id/tvEditItemsBelow"
        android:layout_marginTop="27dp"
        android:ems="10"
        android:inputType="textMultiLine" />

    <Button
        android:id="@+id/btnSave"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/etEditItem"
        android:layout_below="@+id/etEditItem"
        android:layout_marginTop="20dp"
        android:onClick="saveTodoItem"
        android:text="Save" />

</RelativeLayout>
